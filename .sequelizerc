const { resolve } = require("node:path");

module.exports = {
  config: resolve(__dirname, "src", "config", "credentials.js"),
  "migrations-path": resolve(__dirname, "src", "database", "migrations"),
};
